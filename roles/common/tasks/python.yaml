- name: install python packages
  package:
    name: "{{ item }}"
    state: present
  loop:
  - python3
  - gcc
  - python3-devel

- name: install dependencies
  pip:
    requirements: requirements.txt
    executable: pip3
  args:
    chdir: /app